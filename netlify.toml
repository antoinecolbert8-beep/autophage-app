[build]
  command = "CI=false NODE_OPTIONS='--max-old-space-size=4096' ./node_modules/.bin/prisma generate && next build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
